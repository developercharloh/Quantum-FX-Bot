services:
  - type: static
    name: admin-app
    buildCommand: echo "serving pre-built dist"
    staticPublishPath: artifacts/admin-app/dist/public
    branch: main
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
